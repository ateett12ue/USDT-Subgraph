{
  "name": "usdt",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ usdt",
    "create-local": "graph create --node http://localhost:8020/ usdt",
    "remove-local": "graph remove --node http://localhost:8020/ usdt",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 usdt"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
